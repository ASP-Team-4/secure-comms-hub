// const request = require("supertest");
// const app = require("./app.mock");

// describe("GET / agent login", () => {
//   test("should respond with a 200 status code", async () => {
//     const response = await request(app).get("/agent/agent-login");
//     expect(response.header["content-type"]).toEqual("text/html; charset=utf-8");
//     expect(response.statusCode).toEqual(200);
//   });
// });

// describe("POST / agent login", () => {
//   test("should respond with a 200 status code", async () => {
//     const response = await request(app)
//       .post("/agent/Agentlogin")
//       .send({ username: "test_agent", password: "123456" });
//     expect(response.header["content-type"]).toEqual("text/html; charset=utf-8");
//     expect(response.statusCode).toEqual(200);
//   });
// });
